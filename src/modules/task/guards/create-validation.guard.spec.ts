import { CreateValidationGuard } from './create-validation.guard';

describe('CreateValidationGuard', () => {
  it('should be defined', () => {
    expect(new CreateValidationGuard()).toBeDefined();
  });
});
