import { DeleteValidationGuard } from './delete-validation.guard';

describe('DeleteValidationGuard', () => {
  it('should be defined', () => {
    expect(new DeleteValidationGuard()).toBeDefined();
  });
});
